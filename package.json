{
  "name": "isaac-jung-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site for Isaac Jung — Statistics & ML @ Carnegie Mellon",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
